# Benchmark adapter fixtures

These records are synthetic wiring fixtures. They resemble only the structural fields consumed by the adapters and do not copy public benchmark questions, answers, filings, or workbooks.

Fixture-oracle reports are always `publishable: false`. Public datasets remain external-only and require an explicit license review acknowledgement plus a source SHA lock before import.
