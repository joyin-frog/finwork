# CONTEXT — Finwork 领域语言与不变量

> 性质：北极星文档。只写**产品是什么**和**什么不能变**，不写怎么实现。
>
> 日期：2026-08-04
>
> 用法：任何新功能、新 spec、新 PR，先过这里的「防漂移测试」。与本文冲突的方案必须显式指出冲突，不得静默覆盖。

---

## 1. Finwork 是什么

> **一个通用的财务对话入口，加一组有交付保证的任务类型。**

它就是一个会做财务的聊天助手——**通用是对的，也是必须的**。要区分的不是「通用 vs 专用」，而是**哪一层带正确性保证**。

### 两档

| | 对话档 | 交付档 |
| --- | --- | --- |
| 覆盖范围 | **通用** —— 任何财务话题 | 有限 —— 已定义的任务类型 |
| 产出 | 回答、分析、建议、解释、临时算一下 | 正式文件，可复核、能直接用 |
| 正确性 | 模型能力水平，允许「我理解是这样」 | **有保证**：数字对、可追溯、过验收断言 |
| 能力来源 | 模型自带 + 知识库 | 四件套（见第 2 节） |

**升档规则：** 从对话档升到交付档时，若没有对应的任务类型，**必须诚实说明**——「这个我可以帮你分析，但还不能保证给你一个能直接用的文件」。**不得假装有保证。**

### 对话档同时是任务类型的雷达

用户反复问的、反复要的，就是下一个该定义的任务类型。通用层不是要被消灭的东西，是产品的增长入口。

### 真正要避免的

不是「通用」，而是**在没有轨道的地方假装有轨道**——让模型自由写文件，再拿一堆校验器事后抓错。历史财务评测 5/7 超时就是这么来的。

---

## 2. 原子单元：任务类型

Finwork 的最小构建单位是**任务类型**，不是对话、不是功能模块、不是部门职能。

一个任务类型由四件东西绑定定义，缺一不成立：

| 四件套 | 回答的问题 |
| --- | --- |
| **输入契约** | 要什么文件、什么字段；每一项缺失时的降级行为是什么 |
| **领域函数** | 怎么算；确定性代码，不是模型 |
| **输出模板** | 长什么样；结构固定，模型只填内容 |
| **验收断言** | 怎么算做对了 |

### 推论

- **有保证的能力增长 = 增加任务类型。** 不是加提示词、不是换模型、不是加强 harness。（对话档的能力靠模型本身增长，不在此列。）
- **专员 / 知识库 / 技能不是三个并列功能，是一个东西的三个面：**

  ```
  任务类型 ──┬── 归属于谁 → 专员
             ├── 查什么表 → 知识库
             └── 怎么执行 → 技能
  ```

- **按部门职能规划是错的。** 「资金管理」「成本管理」这类是组织架构概念，会互相交叉。例：「12月要付哪些款」不是独立功能，是合同台账的一个输出视图。

### 任务类型从哪来：通用能力 → 提炼

任务类型**不是预先设计出来的**，Finwork 也不可能穷举所有财务问题。它们是从通用能力里长出来的：

```
通用能力跑通  →  观察哪类任务反复出现  →  提炼成领域函数 / 输出模板  →  成为有保证的任务类型
```

这条路径的前提是**通用能力必须先能跑通**。顺序不能反：没有能用的通用层，就没有可提炼的素材，只能靠拍脑袋预设任务类型——那正是过度设计。

**提炼的自动化边界：**

| 环节 | 可否自动 |
| --- | --- |
| 记录每次任务的输入类型 / 步骤 / 产出结构 / 用户改了什么 | ✅ 自动 |
| 发现高频模式并提示「这类任务出现 N 次了，要不要固化」 | ✅ 自动 |
| 从历史记录草拟四件套初稿 | ✅ 自动出草稿 |
| 确认固化 | ⚠️ **必须人**（不变量 2） |
| agent 修改自身代码 | ❌ **不做**，财务领域尤其 |

**触发可以自动，内容需要人点头。** 这已经能让 Finwork 越用越快，且不冒险。

---

## 3. 六条不变量

### 不变量 1 — 模型的能力边界分三档

| | 模型能不能做 | 条件 |
| --- | --- | --- |
| **抽取** | ✅ 可以 | 必须带来源坐标，用户能一键跳回原文核对 |
| **计算** | ❌ 不可以 | 一律走确定性领域函数 |
| **生成文件字节** | ❌ 不可以 | 一律走模板渲染 |

分界线：**抽取可以错，因为错了看得出来（有原文对照）。计算不能错，因为错了看不出来。**

### 不变量 2 — 不确定就问，不许猜

缺口径、缺对照表、缺期间定义 → 挂起、问一句、等回答。

「不可算」「无基准」「待确认」都是**合法结论**，不是失败。用无依据的数字填空才是失败。

**同一条原则在验证侧：能力不可用 ≠ 校验失败。** 本机没装 LibreOffice、引擎不支持某个函数，说明的是「没测成」，不是「测挂了」。这类断言标 `unverifiable`——既不进分子也不进分母，但只要存在就不得宣称通过（收敛到 `needs_review`）。把「没测」记成「测挂了」是评测数据不可信的直接来源：2026-08-05 的 HISTORY-001 交出了 1143 条公式的工作簿，却因一张预览图没渲出来拿到确定性分 0。

> 用户原话：「你直接生成一个 Excel，结果生成的不对，我是不会去核查的。」

### 不变量 3 — 每个数字都能指回来源

不能追溯的数字等于没有。事实层必须带坐标：哪个文件、哪个 sheet、哪个单元格或哪一页。

这是财务区别于其他领域的地方，也是用户敢用的唯一理由。

### 不变量 4 — 自由度和验证成本是同一个东西

给模型多少自由，就要付多少验证成本。**收掉自由比加强验证便宜一个数量级。**

### 不变量 5 — 五层架构，领域能力必须可被 agent 调用

```
输入文件 → ① 事实层：确定性解析，出结构化事实（带来源坐标）
             ↓
         ② 领域层：计算 / 映射 / 勾稽 / 比对（纯函数）
             ↓
         ③ 工具层：把 ①② 暴露成 agent 能调的动作
             ↓
         ④ 渲染层：结构化结果 + 模板 → XLSX / DOCX（固定代码）
             ↓
         ⑤ Agent：理解意图、选任务类型、识别歧义
```

**③ 是历史上最常断的一层。** 领域函数写好了却调不到，模型就只能自由发挥，于是需要一个 coding agent 规模的 harness 去管它。

#### 领域函数分两类，接法不同

| 类型 | 例子 | 谁调用 | 模型是否知情 |
| --- | --- | --- | --- |
| **计算类** | 财务比率、canonical 三表取数 | **模型**（经工具层） | 知情，需主动选用 |
| **校验类** | 业务常识勾稽（占比 > 100%） | **管道**（自动执行） | **不知情，不可绕过** |

**校验类绝不暴露成工具。** 一旦成为模型可选的动作，模型就可以不选——那等于没有。它必须挂在产出管道上强制执行。

**计算类按任务类型拉动接线，不做批量暴露。** `lib/domain/` 有数十个函数，一次性全暴露是在猜哪些有用；由任务类型的四件套决定需要哪几个，接哪几个。

### ①④ 的通用形态：文档通用层

Excel / PDF / DOCX / PPTX 的读写是**通用能力**，不属于任何单个任务类型，一次建好全任务受益。它是通用能力路径的地基。

**读**：结构化定向读取（列出 sheet、取指定范围、取公式、PDF 文本层优先 + 定向 OCR），不做全文 dump。

**写**：有三种做法，不是两种——

| 做法 | 通用性 | 安全性 | 说明 |
| --- | --- | --- | --- |
| 模型写 openpyxl/docx 代码 | 高 | **低** | 历史评测走这条，超时重灾区 |
| 每个任务一个专用模板 | **低** | 高 | 凭证路径走这条，稳但不通用 |
| **模型输出结构化描述 + 通用渲染器** | **高** | **高** | ✅ 正确形态 |

第三种：模型产出「哪个 sheet、哪些行列、哪个单元格什么值/什么公式/什么格式」这样的**中间描述**，由固定渲染器生成文件。DOCX/PPTX 同理（章节、段落、表格的描述）。

好处是叠加的：**通用**（任何表格都描述得出来）、**安全**（文件结构由渲染器保证）、**公式正确**（渲染器写入，lineage 天然完整）、**好验证**（验证描述比验证字节容易一个量级）。

> **关键：自动提炼依赖于此。** 模型写的是代码就无法提炼；写的是结构化描述，则高频描述模式本身就是模板雏形，提炼几乎免费。「通用能力」和「不让模型写代码」不是对立的，是同一件事的两面。

**编辑既有工作簿是有损操作，必须走 XML 层。** openpyxl 的 `load_workbook()` → `save()` 会清空整册公式缓存值（实测真实工作簿 **1164 → 0**，期间什么都没改）。缓存丢失后，含外部链接的公式在本机永远算不回来——那些外部文件根本不在这台机器上。所以「改用户已有的表」用 `patch_workbook`（只重写点名的单元格），不用脚本重写整册；新建空白表不受此限。

> 这条是不变量 4 的实例：允许模型自由 load→save，就得配一台重算引擎去补，而且补不全。收掉这个自由，问题根本不发生。

**实测证据（2026-08-04 历史财务评测，7 个真实 Case）：`bash` 占全部工具调用的 49%–70%**（H001 60/87、H006 47/67、H007 49/89、H004 43/63、H002 23/39、H003 19/38）。模型的大部分时间花在写和跑 Python 脚本上——缺通用渲染层，它只能当 coding agent。这是数据，不是推理。

### 不变量 6 — 不要把「系统需要知道的事实」做成「模型需要执行的动作」

模型会忘、会重复、会挑错时机。凡是系统必须知道的事实（哪个是交付物、验证过没有、进度到哪了），都应当由系统**观察**得到，而不是等模型**声明**。

**典型反例：`finalize_deliverable`。** 把「声明交付物 + 验证 + 锁定副本」三件事绑成一个模型必须记得调用的工具，直接产生三类故障：忘了调（有文件却不算完成）、重复调（同一未变文件反复跑完整验证与重算）、时机错（验证失败 → 修复 → 再调 → 循环）。

后续为它打的补丁——最小交付检查、证据去重、hash 版本化目录、修复指纹——**每一个都正确，但都在补同一个错位**。

**正解：** 在「结构化描述 + 通用渲染器」下，交付物由渲染产生，验证在渲染时同步完成，系统天然知道交付了什么。finalize 从「模型的一次声明」退化为「渲染管道的最后一步」，作为模型工具自然消失。

**2026-08-06 补充（下游传播 + 引擎性能边界）：** 只重算「本次新写入的公式」不够——H002 实测：填完 TB表 后，资产负债表的既有汇总公式（`SUMIFS(TB表!N:N,...)`、`SUM(C37:C57)` 这类跨格/全列引用）缓存仍是模板旧值，读不出真实数字。修法是依赖闭包：从改动点出发按引用关系逐跳传播，找出所有间接受影响的既有公式，一并交给引擎；能算出有效值才覆盖，算不出就保留原缓存并标记待人工核对——判断标准不变，范围从「新写的格子」扩大到「真正受影响的格子」。

闭包实现还发现一个必须处理的细节：OOXML 的共享公式（`<f t="shared">`）里，只有「主格」带完整公式文本，「从格」只有一个索引，缺省会被扫描逻辑当成非公式格漏掉——实测真实模板 1735 个公式格里 443 个是这种从格，漏检比例超过四分之一。展开算法已用 openpyxl 的独立还原结果做过验证（2913/2913 全部一致）。

**引擎有真实的性能边界，不能无条件信任。** 同一份真实模板（302 行 TB表 + 大量全列 SUMIFS 交叉引用）会让 `formulas` 库的图构建卡住——实测 90 秒仍未跑完，且没有任何中间反馈。这很可能是 HISTORY-002 两轮评测均耗尽 40 分钟、finalize 从未成功的真实根因：早期实现对引擎调用完全没有超时保护，一次挂起就吃光整个预算。现已改为子进程执行 + 硬超时（45s），超时按「引擎不可用」处理，不阻塞调用方。**教训：任何调用第三方计算库的地方都要默认它可能对某些输入无限期挂起，超时熔断不是可选项。**

**2026-08-06 再补充（数值/文本类型静默错写）：** 同一轮 H002 复测中发现，模型往 `patch_workbook` 传金额时把数字序列化成了 JSON 字符串（`"2850.8"` 而非 `2850.8`），工具忠实写成了 Excel 文本类型——`SUM`/`ROUND` 等公式会静默跳过文本单元格，合计结果偏小且**不报任何错**，是红线「数值正确性」里最隐蔽的一类。已修：字符串若能安全解析为纯数字（含千分位）就按数值写入，对齐 Excel 自身"直接敲数字进单元格"的默认行为；**带前导零的字符串（`"007"`）保持文本**，因为这类几乎总是账户代码/单号，数值化会丢掉前导零、改变精确匹配语义。

**已知边界，不是回避：** 6 位以上、无前导零的纯数字字符串（如科目代码 `"911010"`）语法上与金额完全无法区分，仍会被转成数值。真实数据里科目代码是模板自带的静态文本，这次改动全部落在数值输入列、从未触碰代码列，所以目前是理论风险，不是已观测到的损害——按不变量 5（证据驱动，不为空想场景写防御代码），暂不加「强制保持文本」的显式开关。如果未来场景变化（模型开始显式改写代码类字段），需要给 `patch_workbook` 的 edit 加一个显式的文本保留开关。

**注意：** 这不作废已建成的交付物验证机制（registry、validator、CompletionEvidence、不可变副本、hash 绑定）——那套逻辑全部保留。变的只是触发方：模型 → 渲染器。

#### 当前处置（2026-08-04 决定，已按实测数据修正）

| 改法 | 内容 | 决定 |
| --- | --- | --- |
| **A** | 同 hash 短路（同一交付物 + 同一 workingSha256 已 delivered 则直接返回，不重跑验证）+ validating 状态加超时 | ✅ 做，但**只覆盖 3/7 Case** |
| B | 系统扫描输出目录、按类型猜哪个是交付物 | ❌ **跳过**。给错位打补丁，且会被 C 废弃 |
| C | 渲染即交付，finalize 作为模型工具下线 | ⏸ 方向，由基线数据决定时点 |

**A 的实测覆盖面（2026-08-04 评测）：** H001 调用 finalize 22 次、H007 19 次、H003 13 次——A 对这三条有效；H004 仅 5 次，收益有限；**H002 与 H006 两条超时 Case 完全没有调用过 finalize，A 对它们零效果**。

**执行顺序：**

```
0. 先确认 agent 硬超时是否有意设为 12 分钟   ← 零成本，但它掩盖一切其他变量
1. 用合理超时跑一次基线                      ← 这才是可比的 before
2. A（预期只改善 H001 / H003 / H007）
3. 通用渲染层（对全部 Case 有效，bash 占比是它的靶子）
```

> **陷阱预警：** 若跳过第 0 步直接做 A 再跑，结果大概率仍是"大部分超时"，容易误判成"A 没用"——真实原因是 A 修的不是瓶颈。

#### 评测证据的位置与读法

- 历史评测报告在 **`/Users/gyro/.codex/worktrees/bfad/finance-agent-public/tests/history-eval/real-reports/`**（不在 Claude worktree 内；`result.json` 位于第 7 层目录，浅层 `find` 会漏掉）。
- **2026-08-04 `full-wide-budget-v3` 批次的 5 条超时，全部卡在 12.0 分钟整**，且掐断时仍在密集调用工具。这是 agent 硬超时所致，**不是模型跑不完**。引用这批数据时不得表述为"harness 不收敛"或"模型能力不足"。
- `judgeStatus: "unavailable"` 目前混淆了两种情况：**服务真不可用** 与 **硬门未过所以 judge 根本没跑**。读汇总时不要当成前者。

---

## 4. 任务类型清单

三种类型的架构性质完全不同，不可混为一谈。

### A. 台账型 — 维护一张活的表，状态会变

| ID | 任务类型 | 产出 |
| --- | --- | --- |
| **T1** | 合同要素台账 | 一行一合同：甲方/乙方/金额/付款节点/开票条款 |
| **T2** | 订单履约台账 | 一行一订单 + 状态：合同签否/开票否/交付否 |
| — | *派生视图*：应付应收时间轴 | 「本月要付哪些款」 |

**台账型必须存数据库，导出成 Excel 给用户看。** 用户的心智模型是「一张 Excel 表」——这对**看**是对的，对**存**是错的：状态要更新、要能查历史、可能多人同时改。

### B. 报告型 — 一次输入，一次产出，无状态

| ID | 任务类型 | 产出 |
| --- | --- | --- |
| **T3** | 经营分析 | 对话结论 → 用户确认 → Excel + Word |
| **T4** | 收入/成本/费用明细分析 | T3 的下钻，不单独成型 |
| **T5** | 研发费用加计扣除合规核查 | 核查清单（含"委外是否备案"这类盲点） |

### C. 订阅型 — 持续运行，联网

| ID | 任务类型 | 说明 |
| --- | --- | --- |
| **T6** | 税收优惠资格筛查 | 企业画像 → 可享政策清单 |
| **T7** | 政策速递 | 架构异类：需定时任务 + 外部数据源 |

> **T6/T7 的可靠性约束：** 其他任务的输入是用户给的文件，错了能追溯；政策类的输入是互联网，过期政策 + 地方口径 + 模型幻觉三重风险叠加，而用户无法核对。
>
> 因此正确形态是：**保证不漏掉线索，每条给出政策原文出处和日期，判断权留给用户或其税务顾问。** 做成「agent 告诉你该怎么筹划」就是埋雷。

---

## 5. 明确不做

- ❌ 给通用 Excel 编辑**贴上交付保证**（可以做，但属对话档产物：不进 finalize、不承诺正确性、明确告知用户）
- ❌ 完整 Excel 公式引擎
- ❌ 在交付档让模型写生成文件的代码
- ❌ 任何「模型说完成了就算完成」的交付路径
- ❌ 在没有对应任务类型时，假装交付物有保证
- ❌ 在任务类型定义清楚之前扩充专员数量
- ❌ 为了少调几次工具牺牲可追溯性

**当前明确推后（有理由，非遗忘）：**

| 项目 | 推后原因 |
| --- | --- |
| 成本**核算** | 需接 ERP / 订单 / 采购 / 库管数据，当前无数据源。注意与成本**分析**区分——后者来自报表，属 T3 |
| 发票管理、薪资计算 | 优先级 |
| 主动提醒推送 | 用户明确决定「先自己看，总览里能看到就行」 |
| 同业基准数据 | 无可靠公开数据源，见 T3 spec |

---

## 6. 防漂移测试

任何新想法，先过两问。

**第一问：这件事属于哪一档？**

- **对话档** → 不需要四件套、不需要门禁、不进 finalize。**别给闲聊套合同**，那是另一种过度设计。
- **交付档** → 继续第二问。

**第二问：这件事是在增加一个任务类型，还是在管住模型的自由？**

- **增加任务类型** → 做，这是产品变强
- **管住模型的自由** → 先问能不能把那个自由直接收掉

阶段状态机、能力预检注册表、判分分层、repair 收敛判定——**全部属于第二类**。收掉自由之后，其中一半会自动消失，剩下一半的规模会小一个量级。

**目标不是一个更强的 harness，是一个不需要那么强 harness 的架构。**

---

## 7. 术语表

| 术语 | 含义 |
| --- | --- |
| 对话档 | 通用财务对话，覆盖无限，**不带正确性保证**，不产出正式交付物 |
| 交付档 | 已定义的任务类型，覆盖有限，**带正确性保证**，产出可复核文件 |
| 升档 | 从对话档进入交付档；无对应任务类型时必须诚实说明 |
| 任务类型 | 交付档的原子构建单位，由四件套定义 |
| 文档通用层 | Excel / PDF / DOCX / PPTX 的通用读写能力，不属于任何单个任务类型 |
| 中间描述 | 模型产出的结构化文件描述（sheet/单元格/公式/章节），由渲染器变成文件 |
| 提炼 | 从通用能力的高频使用模式中固化出任务类型的过程 |
| 四件套 | 输入契约 / 领域函数 / 输出模板 / 验收断言 |
| 抽取 / 计算 / 生成字节 | 模型能力边界的三档（不变量 1） |
| 不可算 | 分母≤0 等边界的合法结论，不得退化为 0 或空 |
| 无基准 | 缺同比/预算/同业数据时的合法结论，不得补估算数 |
| 台账型 / 报告型 / 订阅型 | 任务类型的三种架构性质 |
| 来源坐标 | 数字可追溯到的位置：文件 + sheet + 单元格，或文件 + 页码 |

---

## 相关文档

- [T3 经营分析四件套](docs/spec/task-t3-business-analysis.md)
- [Harness 设计讨论记录](docs/harness-agent-design-discussion.md)（讨论记录，非方案；其结论应按本文第 6 节重估）
