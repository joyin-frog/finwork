openpyxl==3.1.5
pandas==2.2.3
xlsxwriter==3.2.0
xlrd==2.0.1
pdfplumber==0.11.4
pypdf==5.1.0
reportlab==4.2.0
python-docx==1.1.2
python-pptx==1.0.2
defusedxml==0.7.1
# 公式求值引擎:给 patch_workbook 新写入的公式补缓存值。刻意不整册重算——
# 真实工作簿的外部链接指向别人机器上的文件,整册重算会把有效缓存换成错误值。
formulas==1.3.4
lxml==6.1.1
Pillow==12.2.0
pdf2image==1.17.0
markitdown[pptx]==0.1.1
rapidocr-onnxruntime==1.4.4
# 1.20+ dropped Intel macOS wheels; 1.19.2 is the last CPython 3.12 universal2 line
# and keeps OCR available on every release target (macOS arm64/x64 + Windows x64).
onnxruntime==1.19.2
# Electron 43 still runs on macOS 12. Keep native scientific/PDF wheels on
# releases that cover Monterey as well as Intel macOS and Windows x64.
numpy==1.26.4
scipy==1.14.1
opencv-python==4.10.0.84
pypdfium2==4.30.0
