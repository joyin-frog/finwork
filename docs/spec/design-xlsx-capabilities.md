# Design：多 sheet 财务 xlsx 处理能力盘点与补齐

> 状态：盘点已核实（2026-08-05，逐条对照代码），补齐进行中
>
> 前置：[`CONTEXT.md`](../../CONTEXT.md)（两档模型、六条不变量）
>
> 性质：能力现状 + 缺口清单 + 补齐计划。不是承诺全部实现。

---

## 1. 现状盘点

判定口径：✅ 有且通用；🟡 有但只覆盖特定场景；❌ 没有。
**只记录逐条核实过的结论，不含推测。**

### 1.1 分析

| 能力 | 状态 | 实际是什么 |
| --- | --- | --- |
| 读多 sheet、识别表头 | ✅ | `spreadsheetInspect` 返回 sheets / headers / sample_rows / columns |
| 识别期间、科目、金额区块 | 🟡 | 仅三大报表（`parse_statements.py` 按行次取数，兼容列位漂移）；任意表无 |
| 汇总 | 🟡 | `analyze_tabular` 仅 count/sum/avg/min/max + groupBy，且需先把数据变成结构化行 |
| 对账 | 🟡 | 仅银行对账（`bank-recon-batch`） |
| 勾稽检查 | ❌ | 无通用勾稽引擎 |
| 同比 / 环比 | ✅ | `financial-ratios` + business-analysis 三基准列 |
| 结构分析 | 🟡 | 仅四能力那套 |
| 公式错误扫描 | ✅ | `inspect` 全量扫 `#REF!/#DIV/0!` 等 |
| 异常值 / 缺失值 / 重复项 | ❌ | 无通用检测 |
| 跨表不一致 | ❌ | 无 |
| 输出问题清单 | 🟡 | `emit_checklist` 在，内容靠模型 |

### 1.2 编辑

| 能力 | 状态 | |
| --- | --- | --- |
| 修改现有 xlsx | ✅ | `patch_workbook`，XML 层无损，保样式 / 公式 / 缓存 |
| 新增 sheet | ❌ | sheet 不存在直接 `sheet_not_found` |
| 新增辅助列 / 校验列 | ✅ | 即写单元格 |
| 写入公式 | ✅ | 并由 `formulas` 引擎自动补算缓存值 |
| 批量清洗 / 统一格式 / 重命名列 | ❌ | 无 |
| 拆分 / 合并表 | ❌ | 无 |

### 1.3 财务场景

| 能力 | 状态 | |
| --- | --- | --- |
| 三大报表解析 | ✅ | 会小企 / 金蝶导出格式 |
| 科目余额表 | 🟡 | 有 fixture，无专用解析器 |
| 凭证汇总 | ✅ | 金蝶路径最成熟（11 个领域工具） |
| 科目映射 | ✅ | `resolveAccount`：关键词 → 科目码，对照表驱动 |
| 费用台账 / 收入成本分析 | 🟡 | 局部；T3 下钻仍待建 |
| 多公司 / 多期间合并 | ❌ | HISTORY-002 实测扛不动 |
| 报表交叉核对 | ❌ | 无 |

### 1.4 结构与保真

| | 状态 | |
| --- | --- | --- |
| 保留原表结构与样式 | ✅ | `patch_workbook` 的核心保证 |
| 合并单元格 | ✅ | inspect 报 `merged_ranges`，patch 保留 |
| 筛选 / 冻结窗格 | ✅ | inspect 报 `auto_filter` / `frozen_panes`，patch 保留 |
| 复杂公式 | 🟡 | 引擎覆盖常用函数；`IFERROR` + 外部链接算不出 |
| 外部链接 | 🟡 | 能识别、缓存能保住，但本机算不出 |
| 数值精度 | 🟡 | `money.ts` 分单位；引擎回浮点 |
| 多 sheet 引用关系 | ❌ | 无依赖图；patch 只给直接引用的下游清单，不做传递闭包 |
| 隐藏行列 | ❌ | 不感知 |
| 日期格式 | ❌ | 未处理 |
| 数据透视缓存 | ❌ | 未处理 |
| 宏 `.xlsm` | ❌ | 合同明确 `needsMacroPreservation: false` |

### 1.5 形状判断

```
两口深井：三大报表分析 ✅   凭证 / 金蝶 ✅
一层地基：无损编辑 ✅（2026-08-05 建）
中间的通用面：很薄
```

「严格不动原格式、只补几个结果单元格」是目前**唯一有交付保证**的通用能力。
「结构化处理与结果校验」里，**校验强、处理弱**：清洗、重组、拆分合并、新建 sheet 皆空白。

---

## 2. 补齐计划（四项）

按价值排序，均要求：确定性实现 + 单测守护 + 暴露为工具（③工具层）。

### G1 新增 sheet

`patch_workbook` 支持创建工作表：写 sheet XML、注册进 `workbook.xml` / `workbook.xml.rels` / `[Content_Types].xml`。
是 G3 的前置——合并汇总要有地方落。

### G2 跨表勾稽

声明式核对：调用方给出「左值 = 右值（含容差）」清单，代码确定性判定。
不猜规则——规则由任务或用户声明，引擎只负责算和判。
另内建通用恒等式：资产 = 负债 + 所有者权益。

### G3 多表合并汇总

按行标签对齐多个来源的表，输出「一行一科目、一列一来源 + 合计」的矩阵。
**不含抵消**：抵消是合并报表的业务规则，属独立任务类型，不塞进通用能力。

### G4 异常 / 重复 / 缺失检测

对结构化行做确定性检测：重复键、必填缺失、离群值（IQR）、符号异常。
输出问题清单，不做「智能判断」。

---

## 3. 明确不做

- 宏 / VBA 保留
- 数据透视缓存重建
- 样式美化
- 合并报表的抵消逻辑（属任务类型，不属通用能力）
- 任意表的「智能」表头识别（靠猜的结构识别不进交付档）

---

## 4. 验收

四项完成后重跑 HISTORY-001 与 HISTORY-002，对比：交付率、断言通过数、`bash` 调用占比、`patch_workbook` 使用次数。
