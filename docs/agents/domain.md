# Domain docs

本仓库采用 single-context 领域文档布局。

## 探索前读取

- 根目录 `CONTEXT.md`（存在时）：产品领域语言、模块边界和不变量。
- `docs/adr/`：与当前改动相关的架构决策。

文件不存在时继续执行，不主动创建占位文档。领域语言或决策真正形成时，再通过相应文档工作流补充。

## 使用规则

- 测试名、Issue、规格和实现说明使用 `CONTEXT.md` 定义的术语。
- 不随意为已有概念发明同义词。
- 若方案与现有 ADR 冲突，必须显式指出冲突，不能静默覆盖。
